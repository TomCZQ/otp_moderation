import { AxiosInstance, AxiosResponse } from 'axios';
import { Platform, Region } from './enums';
export type GeneralRegion = Platform | Region;
export interface LimiterConfig {
    /** Concurrency for each region */
    concurrency?: number;
    debug?: boolean | 'mock';
}
export declare class RiotRateLimiter {
    private readonly regionRateLimiterMap;
    constructor(axiosInstance: AxiosInstance, config: LimiterConfig);
    execute<T>(generalRegion: GeneralRegion, realPath: string, path: string, query?: Record<string, any>): Promise<AxiosResponse<T>>;
}
