import { Field, Item, Options, Parameter, PrefixMetaProperties, Table } from './types';
export declare class CargoClient<P extends string = ''> {
    private readonly metadataPrefix;
    readonly axiosInstance: import("axios").AxiosInstance;
    constructor({ metadataPrefix }?: Options<P>);
    private spaceToUnderscore;
    private convert;
    private addPrefixToMetadata;
    query<T extends Table, Fields extends Field<T>, LeftField extends Field<T>>(parameter: Parameter<T, Fields, LeftField>): Promise<{
        data: (P extends "" ? Item<Fields> : PrefixMetaProperties<Item<Fields>, P>)[];
        status: number;
        statusText: string;
        headers: any;
        config: import("axios").AxiosRequestConfig;
        request?: any;
    }>;
}
