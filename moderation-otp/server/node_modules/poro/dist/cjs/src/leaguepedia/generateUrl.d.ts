import { Field, Parameter, Table } from './types';
export declare function generateUrl<T extends Table, LeftField extends Field<T>>({ tables, fields, where, joinOn, groupBy, having, orderBy, limit, offset, format, }: Parameter<T, Field<T>, LeftField>): Promise<string>;
